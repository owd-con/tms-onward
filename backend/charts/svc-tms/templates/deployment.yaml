apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-tms
  labels:
    app: svc-tms
    devspace.dev/app: svc-tms
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: svc-tms
  template:
    metadata:
      labels:
        app: svc-tms
        devspace.dev/app: svc-tms
    spec:
      containers:
        - name: svc-tms
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: shared-env
          env:
            {{- range .Values.env }}
            - name: {{ .name }}
              value: "{{ .value }}"
            {{- end }}
          ports:
            - containerPort: {{ .Values.containerPort }}
            - containerPort: {{ .Values.containerGRPCPort }}